{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "database": "${workspaceFolder:LocationSystem}/server-app/locations.db",
            "name": "Locations"
        }
    ]
}