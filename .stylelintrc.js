// .stylelintrc.js
module.exports = {
  extends: [
    "stylelint-config-standard", // A good baseline config
    // 'stylelint-config-prettier' (if using Prettier for CSS)
  ],
  rules: {
    // Add or override Stylelint rules here
  },
};
